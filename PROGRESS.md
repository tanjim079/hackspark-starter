# Progress Tracker

## Setup & Initialization
- Initialized local workspace.
- Pulled initial codebase template from the remote repository (`https://github.com/tanjim079/hackspark-starter.git`).

## Environment Configuration
- Configured environment variables by copying `.env.example` to `.env`.

## User Service
- Resolved a missing script issue by adding a `"dev": "node index.js"` command to `user-service/package.json`.
- Installed Node.js dependencies (`npm install`) inside the `user-service` directory.
- Successfully verified the `user-service` can run locally on port `8001` using `npm run dev`.
